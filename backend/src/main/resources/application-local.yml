spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:metrostamp}
    username: ${DB_USERNAME:metrostamp}
    password: ${DB_PASSWORD:metrostamp}
  data:
    mongodb:
      uri: mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DATABASE:metrostamp_logs}

