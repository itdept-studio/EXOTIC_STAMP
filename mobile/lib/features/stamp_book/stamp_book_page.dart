import 'package:flutter/material.dart';

import '../../core/widgets/placeholder_scaffold.dart';

class StampBookPage extends StatelessWidget {
  const StampBookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScaffold(
      title: 'Stamp Book',
      description: 'So huu stamp cua nguoi dung va tien do reward.',
    );
  }
}

